# Streamlit Community Cloud deployment dependencies
# Rebuild marker: core 0.11.0 (OCR A–Z: extraction/ocr + pypdf/pytesseract, scan→bekreftelse→kontroll) — bump to force a
# fresh pip environment on Cloud whenever core/models or the core API changes.
# Cloud only reinstalls dependencies when a dependency file changes; a version
# bump in pyproject.toml alone is NOT seen unless this file also changes.
sqlmodel>=0.0.16
pyyaml>=6.0
fpdf2>=2.8
defusedxml>=0.7
# OCR-inntak (Funksjon 3.5). pypdf is pure Python (PDF text layer works everywhere).
# pytesseract needs the tesseract BINARY — see packages.txt; without it image OCR degrades
# honestly with a message instead of crashing.
pypdf>=4.0
pytesseract>=0.3.10
pillow>=10.0
streamlit>=1.35
# install this repo as a package (core) - required on Streamlit Cloud
.
